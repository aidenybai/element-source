import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.elementsource.svelte",
  appName: "Element Source Svelte",
  webDir: "dist",
};

export default config;
